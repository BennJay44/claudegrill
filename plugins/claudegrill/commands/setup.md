---
description: Check whether the local ClaudeGrill bridge is ready
argument-hint: ""
---

Run:

```bash
"${CODEX_HOME:-$HOME/.codex}/agent-bridge/claudegrill" setup
```

Use this before the first review to confirm that Claude Code, the plugin package, and the bridge queue are available.
